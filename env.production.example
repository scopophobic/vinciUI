# VinciUI Production Environment Variables
# Copy this file to .env.local and fill in your actual values

# ==============================================
# FRONTEND ENVIRONMENT VARIABLES (Public)
# ==============================================
# These are exposed to the client (must have VITE_ prefix)

# Your Gemini API key from Google AI Studio
VITE_GEMINI_API_KEY=your_gemini_api_key_here

# Google OAuth Client ID (public, safe to expose)
VITE_GOOGLE_CLIENT_ID=your_google_client_id_here.apps.googleusercontent.com

# ==============================================
# BACKEND ENVIRONMENT VARIABLES (Private)
# ==============================================
# These are server-side only and NOT exposed to the client

# Database connection (AWS RDS PostgreSQL)
DATABASE_URL=postgresql://username:password@host:5432/database

# Google OAuth credentials
GOOGLE_CLIENT_ID=your_google_client_id_here.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=GOCSPX-your_google_client_secret_here

# Gemini API key (server-side backup)
GEMINI_API_KEY=your_gemini_api_key_here

# JWT secret for session tokens (generate a long random string)
JWT_SECRET=your_super_long_random_jwt_secret_here_make_it_very_secure

# Base URL for OAuth redirects
BASE_URL=http://localhost:5173

# Environment
NODE_ENV=development

# ==============================================
# PRODUCTION DEPLOYMENT VARIABLES
# ==============================================
# For Vercel deployment, set these in your Vercel dashboard:
# - All variables above
# - BASE_URL=https://your-domain.vercel.app
# - NODE_ENV=production

